# Centralized Analytics Dashboard (Stitch)

**Project:** Centralized Analytics Dashboard (`9134559040488075017`)  
**Screen:** Analytics Dashboard (`543b85587b5d404c89219a624e23d46a`)

## Assets

| File | Description |
|------|-------------|
| `analytics-dashboard.jpg` | Screen screenshot (JPEG, ~512×488 preview from Stitch) |
| `analytics-dashboard.html` | Generated HTML (Tailwind CDN + Inter + Material Symbols) |
| `screen.json` | Raw `get_screen` metadata (title, dimensions, device type, file refs) |

## Notes

- Fetched via Stitch MCP (`get_screen`) using `x-goog-api-key` (not `Authorization: Bearer`).
- HTML is a self-contained Stitch export suitable as a visual/layout reference for LifeOS dashboard analytics UI.
