{
  "name": "@lifeos/mcp-server",
  "version": "0.1.0",
  "description": "MCP server that wraps the LifeOS Core REST API — connect LifeOS to any MCP-compatible AI client.",
  "type": "module",
  "bin": {
    "lifeos-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
