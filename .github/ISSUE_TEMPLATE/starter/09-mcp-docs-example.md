---
title: "docs: add secure MCP client config example"
labels: ["kind: documentation", "good first issue", "area: mcp", "help wanted"]
---

## Context
`docs/mcp.md` should show a minimal Claude Desktop / Cursor config using env for the key (not a committed secret).

## Expected result
JSON snippet with `"SOPHIONOS_API_KEY": "…from env…"` style placeholders only.

## Test
- No real `sop_` keys in the PR.
