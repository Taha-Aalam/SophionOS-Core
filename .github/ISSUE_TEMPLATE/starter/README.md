# Starter issues (Phase A)

Copy any file into a GitHub Issue (or use `gh issue create --body-file …`).
All use synthetic/demo context only. Labels are suggestions for maintainers.

Maintainers: open these before public announcement so `good first issue` is non-empty.
